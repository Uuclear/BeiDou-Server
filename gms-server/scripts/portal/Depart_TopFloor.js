
/**
 * 脚本类型：传送门
 * 对象 ID：Depart_TopFloor
 * 功能描述：地图传送门入口脚本。
 */
function enter(pi) {
    pi.openNpc(1052125); //It is actually suppose to open the npc, because it leads to a boss map
    return true;
}